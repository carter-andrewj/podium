{
  "name": "podium-client",
  "homepage": "http://demp.podium-network.com/",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "ajv": "^6.7.0",
    "immutable": "^4.0.0-rc.12",
    "radixdlt": "^1.0.6-alpha",
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-easy-crop": "^1.7.0",
    "react-scripts": "^2.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "aws s3 sync ./build s3://demo.podium-network.com/",
    "build-and-deploy": "npm run build && npm run deploy",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "alias": {
    "settings": "/src/settings.js",
    "utils": "/src/utils.js"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
