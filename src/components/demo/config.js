const Settings = {

	// ApplicationID: "podiumLIVE",
	ApplicationID: "podiumALPHA-4",		// Radix Identifier for the app,
										//	(increment to reset)

	timeout: 10000						// Dormant time before an open radix
										// 		subscription is killed

}

export default Settings;